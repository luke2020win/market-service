package com.joiest.market.company.authentication.api;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MarketCompanyAuthenticationApiApplication {

    public static void main(String[] args) {
        SpringApplication.run(MarketCompanyAuthenticationApiApplication.class, args);
    }
}
